# ============================================
# requirements.txt
# --------------------------------------------
# 项目依赖清单（含版本锁定与用途说明）
# - 若你已有环境可用，不强制升级；本文件用于快速复现/部署。
# - 安装：python -m pip install -r requirements.txt
# - 运行：python -m app.server
# ============================================

# -------------------------
# Web 框架与运行时
# -------------------------
fastapi==0.139.2           # 轻量 Web 框架（路由、请求校验、OpenAPI 文档）
uvicorn[standard]==0.51.0  # ASGI 服务器（本地开发使用）

# -------------------------
# 数据模型与工具
# -------------------------
pydantic==2.13.4           # 数据校验与模型（FastAPI 依赖）
python-multipart==0.0.32   # 数据集 CSV 上传表单解析
typing-extensions==4.16.0  # 类型补全（某些环境需要）

# -------------------------
# 数值与时间处理（用于环境、统计、预测与控制基线）
# -------------------------
numpy==1.26.4              # 基础数值计算（P95、聚合等）
scipy==1.13.1              # MPC 基线的约束数值优化
pandas==3.0.3              # Stable-Baselines3 日志与实验结果处理
python-dateutil==2.9.0.post0  # 解析 ISO 时间、相对时间计算

# -------------------------
# 真实强化学习与控制基线
# -------------------------
# Intel macOS stopped receiving security-fixed upstream Torch wheels. Do not
# silently install its vulnerable 2.2.x ceiling; use the Linux container on
# that legacy host. Supported native platforms and the container use 2.13.0.
torch==2.13.0; sys_platform != "darwin" or platform_machine != "x86_64"
gymnasium==0.29.1          # 港口运营环境标准接口
stable-baselines3==2.9.0; sys_platform != "darwin" or platform_machine != "x86_64"
sb3-contrib==2.9.0; sys_platform != "darwin" or platform_machine != "x86_64"

# -------------------------
#（可选）前端静态构建/内容压缩（当前未使用）
# brotli==1.1.0
# orjson==3.10.7           # 更快的 JSON 序列化（如需，可在 FastAPI Response 中启用）

# -------------------------
# 开发与诊断（可选）
# -------------------------
httpx==0.28.1              # Ops Copilot 可选 LLM 适配与 FastAPI 测试客户端
requests==2.34.2           # TOS / AIS / 潮汐 / 市场 REST 适配器
