# 独立模块导航与验收

2026-09-12，本地开发环境验收。

首页保留总览与顶部菜单。进入任一模块后，完整的 13 项顶部菜单始终保留，可直接切换其他模块；原页内模块同时显示模块名称和“返回主界面”。每次只显示一个模块，各模块继续使用原有数据、控件及证据，不复制业务 DOM，不重新训练或更换策略。

## 模块范围

| 菜单 | 独立模块数 | 验收 |
| --- | ---: | --- |
| 管理驾驶舱 | 3 | 驾驶舱、平台地图、开发生态逐项打开和返回 |
| 多港区 | 1 | 单独显示画像与迁移准备度 |
| ESG / 合规 | 2 | ESG → 合规跳转、参数保留、浏览器后退 |
| 数字孪生 | 5 | Twin 3D、Story、KPI、实时视图、核心孪生逐项打开；图表有有效尺寸 |
| 策略 → 执行 | 12 | 逐项打开；RL Ops、TwinLab 内部页签可切换；五个专项策略证据按钮正常读取 |
| 审计 / OpsX | 6 | 可信度、监控、OpsX、外部信号、MLOps、治理逐项打开和返回 |

共 29 个原页内入口。运营副驾、V3、RL 面板、联动中枢、未来决策舱、接口文档继续使用各自路径，并共享同一套顶部菜单。菜单通过 `/ui/module-menu` 从首页导航提取，首页预存与会话缓存保证重数据加载期间仍可切换。独立页在同一标签内跳转，旧文档关闭；返回首页使用首页地址，避免误回到上一个独立模块。未来决策舱另保留“返回策略编排”。接口文档在生产配置下仍关闭。

## 状态与图表

- 保留 `/#hvac-section` 等既有链接；支持刷新直达、前进、后退。未知或损坏片段安全返回首页。
- 同一文档内切换不重建表单或重置内部页签。已验证选择 PPO 后进入执行，`execSid` 与所选策略一致；经审计、孪生、首页往返仍保留选择。刷新整页继续使用原有页面初始化规则。
- 离开模块时关闭其可见页面：PortViz 动画和定时拉流暂停，Story 暂停并保留进度，THREE 与本地 Canvas 停止隐藏渲染。慢响应不会重新启动已关闭的播放；共享证据读取和已提交的后端训练保持原有生命周期。
- 路由仅控制自己的隐藏和布局类，不修改业务控件的 `disabled`、内部页签样式、API 权限或策略准入。
- 原页面的许多模块嵌套在实时视图与策略编排中。路由保留祖先节点，清除祖先布局外观并隐藏无关分支，防止隐藏父容器时连带隐藏目标模块。
- 打开模块时重算可见 ECharts 和 Canvas 尺寸；隐藏的专项证据图推迟到可见时绘制。聚合预测图和外部信号图通过缓存的真实响应重绘，不发起额外业务计算或覆盖详情。
- 后台证据完成后不抢占模块或滚动位置。已核验首页加载提示正常收起，小懿形象状态为 ready，图片自然宽度 371。
- 桌面与 720px 窗口实测；窄窗口菜单换行，执行页宽度 696px，返回按钮完整位于视口内。测试后已恢复原视口。

## 检查结果与复现

完整回归：296 项通过，132.520 秒。最后 V3 返回首页、菜单缓存及重试调整后，68 项针对性检查通过，23.198 秒。JavaScript 生命周期检查覆盖请求去重、离开时停止定时器、慢响应竞态、保留手动暂停/播放进度、避免重复渲染循环及跨模块进入 V3 后返回首页；菜单行为检查覆盖提前请求、缓存、重试、页面关闭与 BFcache 恢复。独立审查发现的菜单与模态遮罩叠层冲突已修复。

```bash
.venv312/bin/python -m unittest discover -s tests -v
.venv312/bin/python -m unittest tests.test_module_navigation tests.test_maturity tests.test_ui_linkage tests.test_ui_control_inventory_v6 -v
node tests/js/module_lifecycle.cjs
# 需要可用的 Playwright 与 Chromium；也可指定 PLAYWRIGHT_CHANNEL=chrome。
node tests/js/standalone_navigation.cjs
.venv312/bin/python -m uvicorn app.server:app --host 127.0.0.1 --port 8000
```

浏览器已逐项完成 29 个原页内模块直接互相切换：顶部菜单始终可见，每次只应有一个非布局容器的模块可见。Story 实际播放后切到驾驶舱，再返回 Story，已确认播放暂停且进度保留。点击“返回主界面”应回到总览顶部。另复核策略→执行/审计/孪生、ESG→合规、直接访问 `/#hvac-section` 后刷新、浏览器前进后退、证据加载期间快速返回首页。

六个独立页已逐页验证 13 项菜单、当前项高亮与下拉操作；实际链路包括运营副驾 → RL 面板 → 联动中枢 → 未来决策舱 → 接口文档 → 管理驾驶舱，以及 Story → V3 → 首页。桌面滚动 3191px 后 V3 菜单仍位于顶部；详情遮罩正确覆盖菜单。720px 窗口中主模块无横向溢出，独立页菜单横向滚动、下拉完整可见，并可直接切到 HVAC。最后恢复桌面视口和首页，浏览器错误日志为空。

本次是本地页面改造与回归验收，不构成现场验收或生产控制授权；没有执行发布操作。
