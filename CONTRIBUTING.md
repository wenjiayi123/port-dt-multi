# Contributing

欢迎提交可复现、来源清楚的改进。

1. 不要把模拟数据描述为实时或实测数据。
2. 新增指标必须同时说明公式、单位、时间窗口和来源。
3. 新增算法必须记录实现库、版本、随机种子、训练步数、数据集哈希和留出集结果。
4. 训练代码不得调用渲染器或读取测试切分。
5. 外部接口失败应返回明确错误或空状态，不能静默生成业务指标。
6. 新增生产执行器必须保持默认关闭，并经过人工确认、安全约束和审计记录。
7. 数据集必须提供许可、责任人、时区、预期用途和官方/派生列边界。
8. 改动奖励、环境、评测、模型晋级或执行安全边界时，需在 PR 中附对应的回归测试和风险说明。

提交前运行：

```bash
python -m compileall -q app scripts tests
python -m unittest discover -s tests -v
python -m scripts.rl_smoke_test --steps 64
```

不要提交 `data/rl/runs`、令牌、生产数据或软著材料。安全漏洞不得使用公开 Issue，请按 `SECURITY.md` 报告。
